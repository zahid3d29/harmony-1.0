we need to make three type of settings option for theme user.

1. Theme settings (ex: background: red;)
2. Customize panel settings (ex: color)
3. Package/Sets Settings ( will be available on Theme settings panel and Theme Customize panel) (ex: color-scheme)


1. Theme settings: Where user can customize theme style from theme settings panel. There will be style sets like "Color schema" or single obeject style like "Blog Card" style etc.

2. Customize panel Settings: Which will be available for each sections of theme customize panel. User can see this customization options of each sections and they can modify. example: Announcement bar section will have announcement 'text' style changing options, bar width/height/alignment options etc.

3. Package/Sets: This will be a most unique options for our theme. A set of cuastomize options together will be available on each sections and also theme settings panel. Example: A color scheme options for a section each elements color. currently there are colors scheme group we can use for background, text, button color. But, also we can make size option set there will be font size, weight, style, height etc. 

If they change a set that will effect full sections and their elements.


common schema: {
  "class": "harmony_section-class",
  "disabled_on": {
    "groups": ["header", "footer"]
  },


}




Theme settings: 


Home: Banner{
  div.subheading:
    size: 14px, 19px;

  h1.heading:
    size: 90px, 81px;

  div.content:
    size: 18px, 22px;

  span.sale_badge:
    size: 14px, 19px;
}

Home: Testimonial{
  div.subheading:
    size: 14px, 19px;

  h3.heading:
    size: 37px, 43px;

  div.content:
    size: 18px, 22px;

  span.sale_badge:
    size: 14px, 19px;
}

Home: Why Swiss{
  h3.heading:
    size: 31px, 37px;

  div.description:
    size: 12px, 18px;

  div.content:
    size: 14px, 19px;

  span.step:
    size: 14px, 19px;
}

Home: Featured collection Tab{
  h2.tab-name:
    size: 50px, 62px;
}

Home: Featured Products{
  h2.section-heading:
   size: 50px, 62px;

  .tab-name:
    size: 17px, 23px;

  h4.product-title:
    size: 16px, 22px;

  span.collection-name:
    size: 14px, 19px;

  h4.product-price:
    size: 13px, 18px;

  span.sale-badge:
    size: 14px, 19px;
}

{% style %} 

  text-style{
    font-family: "NeuzeitS-Book"; ( theme settings )
    font-weight: normal; ( theme settings )
    font-style: normal; ( theme settings )

    color: #000000; ( theme section settings color_schemes )
    background: #D0BAAC; ( theme section settings color_schemes )
    
    font-size: 17px; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .lg )
    line-height: 13px; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .leading-1 )
    text-align: center; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .text-center )
  }

  text-style-1 { lg font-bold leading-1 text-normal text-center }

{% endstyle %}



{% comment %} Product Grid style  {% endcomment %}
section color scheme = harmony__color-{{ settings.section_color_scheme }} 
product card color scheme = harmony__color-{{ settings.product_card_color_scheme }}
 => Product overlay color scheme
Badge color Scheme: harmony__color-{{ settings.badge_color_scheme }}


#F3FFD1
0.00%
RGBA(252,244,164,0 )
{% comment %} Header Font and color settings  {% endcomment %}

// tailwind.config.js
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        'neuzeit-book': ['"NeuzeitS-Book"', 'sans-serif'],
        'neuzeit-office': ['"Neuzeit Office"', 'sans-serif'],
        'roboto-mono': ['"Roboto Mono"', 'monospace'],
        'neue-haas': ['"Neue Haas Unica W1G"', 'sans-serif'],
      },
      letterSpacing: {
        'extra-wide': '20px',
        'negative-wide': '-40px',
        'medium-wide': '7.33px',
        'roboto-wide': '7.33px',
      },
      colors: {
        'custom-faf2dc': '#faf2dc',
        'custom-000000': '#000000',
        'custom-f7f4ed': '#f7f4ed',
        'custom-fffaf4': '#fffaf4',
      },
    },
  },
  plugins: [],
}



theme name prefix harmony__

section name = footerbenifits_color_scheme
text-[11px] leading-1 font-normal
text-[12px] leading-1 font-normal

{% comment %} All used font size in XD Design. {% endcomment %}
text-[11px] leading-1 font-normal
text-[12px] leading-1 font-normal
font-size: 13px;
font-size: 14px;
font-size: 15px;
font-size: 16px;
font-size: 17px;
font-size: 18px;
font-size: 19px;
font-size: 20px;
font-size: 25px;
font-size: 26px;
font-size: 28px;
font-size: 30px;
font-size: 31px;
font-size: 32px;
font-size: 33px;
font-size: 35px;
font-size: 37px;
font-size: 40px;
font-size: 42px;
font-size: 45px;
font-size: 50px;
font-size: 52px;
font-size: 67px;
font-size: 80px;
font-size: 90px;
font-size: 95px;
font-size: 142px;

{% comment %} Color scheme for section  {% endcomment %}
harmony__color-text-{{ section.settings.header_color_scheme }} 
harmony__color-text-secondary{{ section.settings.header_color_scheme }} 
harmony__color-background-{{ section.settings.header_color_scheme }} 
harmony__color-border-{{ section.settings.header_color_scheme }}
harmony__color-primary-button-background-{{ section.settings.header_color_scheme }}
harmony__color-primary-button-label-{{ section.settings.header_color_scheme }}
harmony__color-primary-button-border-{{ section.settings.header_color_scheme }}
harmony__color-secondary-button-background-{{ section.settings.header_color_scheme }}
harmony__color-secondary-button-label-{{ section.settings.header_color_scheme }}
harmony__color-secondary-button-border-{{ section.settings.header_color_scheme }}
harmony__color-border-{{ section.settings.header_color_scheme }}
harmony__color-icon-{{ section.settings.header_color_scheme }}
harmony__color-link-{{ section.settings.header_color_scheme }}

{% if settings.product_card_style == 'card' %} harmony__color-text-{{ settings.product_card_color_scheme }} {% else %} harmony__color-text-{{ section.settings.collection_color_scheme }} {% endif %}


{{ settings.logo | image_url: width: '60' | image_tag }}


{% comment %} Using the placeholder_svg_tag filter
It’s really easy to use Shopify’s placeholder SVGs, since including them is a breeze with the placeholder_svg_tag filter.

Simply include the placeholder name, followed by the placeholder_svg_tag filter: {% endcomment %}
{% if block.settings.icon != blank %}
  {{ block.settings.icon | image_url: width: '35' | image_tag: class: 'h-[24px] w-[35px] object-contain' }}
{% else %}
  {{ 'image' | placeholder_svg_tag: 'w-50' }}
{% endif %}


.harmony__footerbenifits-base {
  @apply text-[14px] leading-[20px] font-normal;
}
.harmony__footerbenifits-small {
  @apply text-[12px] leading-[16px] font-normal;
}
.harmony__footerbenifits-large {
  @apply text-[16px] leading-[24px] font-normal;
}


{% if settings.heading_font_size == 'sm' %}harmony__page-header-sm{% elsif settings.heading_font_size == 'lg' %}harmony__page-header-lg{% else %}harmony__page-header-theme-default{% endif %}

{% if settings.body_font_size == 'sm' %}harmony__page-header-caption-sm{% elsif settings.body_font_size == 'lg' %}harmony__page-header-caption-lg{% else %}harmony__page-header-caption-theme-default{% endif %}

{% if settings.body_font_size == 'sm' %}harmony__page-header-desc-sm{% elsif settings.body_font_size == 'lg' %}harmony__page-header-desc-lg{% else %}harmony__page-header-desc-theme-default{% endif %}

  text-[24px] leading-1 md:text-[36px] md:leading-1 lg:text-[50px] lg:leading-[96px]

npx tailwindcss -i ./assets/style.css -o ./assets/output.css --watch