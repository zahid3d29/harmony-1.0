we need to make three type of settings option for theme user.

1. Theme settings
2. Customize panel settings
3. Package/Sets Settings ( will be available on Theme settings panel and Theme Customize panel)


1. Theme settings: Where user can customize theme style from theme settings panel. There will be style sets like "Color schema" or single obeject style like "Blog Card" style etc.

2. Customize panel Settings: Which will be available for each sections of theme customize panel. User can see this customization options of each sections and they can modify. example: Announcement bar section will have announcement 'text' style changing options, bar width/height/alignment options etc.

3. Package/Sets: This will be a most unique options for our theme. A set of cuastomize options together will be available on each sections and also theme settings panel. Example: A color scheme options for a section each elements color. currently there are colors scheme group we can use for background, text, button color. But, also we can make size option set there will be font size, weight, style, height etc. 

If they change a set that will effect full sections and their elements.


common schema: {
  "class": "harmony_section-class",
  "disabled_on": {
    "groups": ["header", "footer"]
  },


}




Theme settings: 


Home: Banner{
  div.subheading:
    size: 14px, 19px;

  h1.heading:
    size: 90px, 81px;

  div.content:
    size: 18px, 22px;

  span.sale_badge:
    size: 14px, 19px;
}

Home: Testionial{
  div.subheading:
    size: 14px, 19px;

  h3.heading:
    size: 37px, 43px;

  div.content:
    size: 18px, 22px;

  span.sale_badge:
    size: 14px, 19px;
}

Home: Why Swiss{
  h3.heading:
    size: 31px, 37px;

  div.description:
    size: 12px, 18px;

  div.content:
    size: 14px, 19px;

  span.step:
    size: 14px, 19px;
}

Home: Featured collection Tab{
  h2.tab-name:
    size: 50px, 62px;
}

Home: Featured Products{
  h2.section-heading:
   size: 50px, 62px;

  .tab-name:
    size: 17px, 23px;

  h4.product-title:
    size: 16px, 22px;

  span.collection-name:
    size: 14px, 19px;

  h4.product-price:
    size: 13px, 18px;

  span.sale-badge:
    size: 14px, 19px;
}

{% style %} 

  text-style{
    font-family: "NeuzeitS-Book"; ( theme settings )
    font-weight: normal; ( theme settings )
    font-style: normal; ( theme settings )

    color: #000000; ( theme section settings color_schemes )
    background: #D0BAAC; ( theme section settings color_schemes )
    
    font-size: 17px; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .lg )
    line-height: 13px; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .leading-1 )
    text-align: center; ( theme settings and section settings. section-settings will be tailwind classes of package like.. .text-center )
  }

  text-style-1 { lg font-bold leading-1 text-normal text-center }

{% endstyle %}




{% comment %} Product Grid style  {% endcomment %}

section color scheme = harmony__color-{{ settings.section_color_scheme }} 
product card color scheme = harmony__color-{{ settings.product_card_color_scheme }}
 => Product overlay color scheme
Badge color Scheme: harmony__color-{{ settings.badge_color_scheme }}


{% comment %} Product Grid products_per_row  {% endcomment %}

products_per_row = 3 -> "grid-cols-1 sm:grid-cols-2 md:grid-cols-3"

products_per_row = 4 -> "grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4"


{%- if section.settings.products_per_row == '3' -%} grid-cols-1 sm:grid-cols-2 md:grid-cols-3 {%- else -%} grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 {%- endif -%}

{% comment %} Show/Hide filter in sidebar  {% endcomment %}


                        {% comment %} Nav with multi lavel menu items  {% endcomment %}
                        <div class="main__menu-item">
                          <div class="py-4">
                            <ul role="nav__item-list" class="space-y-4 pb-6 text-[18px] leading-[32px] text-white">
                              {% assign mob-menu = section.settings.mob_menu %}
                              {% for main_link in linklists[mob-menu].links -%}
                                {% if main_link.links.size == 0 %}
                                  <li>
                                    <a href="{{ main_link.url }}">{{ main_link.title }}</a>
                                  </li>
                                {% elsif main_link.links.size > 0 %}
                                  <li>
                                    <button
                                      type="button"
                                      class="flex w-full items-center justify-between bg-transparent text-[18px] leading-[41px] text-white  hover:opacity-70 hover:underline"
                                    >
                                      <span>{{ main_link.title }}</span>
                                      <span class="ml-6 flex items-center">
                                        <!-- Expand icon, show/hide based on section open state. -->
                                        <svg
                                          class="-rotate-90"
                                          width="24"
                                          height="24"
                                          viewBox="0 0 24 24"
                                          fill="none"
                                          stroke="currentColor"
                                          stroke-width="1.3"
                                          stroke-linecap="round"
                                          stroke-linejoin="round"
                                          class="icon feather feather-chevron-down"
                                          aria-hidden="true"
                                          focusable="false"
                                          role="presentation"
                                        >
                                          <path d="m6 9 6 6 6-6"></path>
                                        </svg>
                                        <!-- Collapse icon, show/hide based on section open state. -->
                                        <svg
                                          width="24"
                                          height="24"
                                          viewBox="0 0 24 24"
                                          fill="none"
                                          stroke="currentColor"
                                          stroke-width="1.3"
                                          stroke-linecap="round"
                                          stroke-linejoin="round"
                                          class="icon feather feather-chevron-down"
                                          aria-hidden="true"
                                          focusable="false"
                                          role="presentation"
                                        >
                                          <path d="m6 9 6 6 6-6"></path>
                                        </svg>
                                      </span>
                                    </button>
                                    <div class="pl-4">
                                      <ul class="nav__item-list space-y-4 text-[18px] leading-[32px] text-white">
                                        {% for child_link in main_link.links %}
                                          {% if child_link.links.size == 0 %}
                                            <li>
                                              <a href="{{ child_link.url }}">{{ child_link.title }}</a>
                                            </li>
                                          {% elsif child_link.links.size > 0 %}
                                            <li>
                                              <div class="nav__item-child">
                                                <ul class="nav__item-list-child space-y-4 text-[18px] leading-[32px] text-white">
                                                  <li>
                                                    <button
                                                      type="button"
                                                      class="flex w-full items-center justify-between bg-transparent text-[18px] leading-[41px] text-white  hover:opacity-70 hover:underline"
                                                    >
                                                      <span>{{ child_link.title }}</span>
                                                      <span class="ml-6 flex items-center">
                                                        <!-- Expand icon, show/hide based on section open state. -->
                                                        <svg
                                                          class="-rotate-90"
                                                          width="24"
                                                          height="24"
                                                          viewBox="0 0 24 24"
                                                          fill="none"
                                                          stroke="currentColor"
                                                          stroke-width="1.3"
                                                          stroke-linecap="round"
                                                          stroke-linejoin="round"
                                                          class="icon feather feather-chevron-down"
                                                          aria-hidden="true"
                                                          focusable="false"
                                                          role="presentation"
                                                        >
                                                          <path d="m6 9 6 6 6-6"></path>
                                                        </svg>
                                                        <!-- Collapse icon, show/hide based on section open state. -->
                                                        <svg
                                                          width="24"
                                                          height="24"
                                                          viewBox="0 0 24 24"
                                                          fill="none"
                                                          stroke="currentColor"
                                                          stroke-width="1.3"
                                                          stroke-linecap="round"
                                                          stroke-linejoin="round"
                                                          class="icon feather feather-chevron-down"
                                                          aria-hidden="true"
                                                          focusable="false"
                                                          role="presentation"
                                                        >
                                                          <path d="m6 9 6 6 6-6"></path>
                                                        </svg>
                                                      </span>
                                                    </button>
                                                    <div class="pl-4">
                                                      <ul
                                                        role="nav__item-list"
                                                        class="space-y-4 text-[18px] leading-[32px] text-white"
                                                      >
                                                        {% for grand_child in child_link.links %}
                                                          <li>
                                                            <a href="{{ grand_child.url }}">{{ grand_child.title }}</a>
                                                          </li>
                                                        {% endfor %}
                                                      </ul>
                                                    </div>
                                                  </li>
                                                </ul>
                                              </div>
                                            </li>
                                          {% endif %}
                                        {% endfor %}
                                      </ul>
                                    </div>
                                  </li>
                                {% endif %}
                              {% endfor %}
                            </ul>
                          </div>
                        </div>